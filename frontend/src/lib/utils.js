export function formatMessageTime(date) {
    return new Date(date).toLocaleTimeString("en-US", {
      hour: "2-digit",
      minute: "2-digit",
      hour12: false,
    });
  }

  //we are taking the date and we are formatting it to the time string
  //and we are returning the time string
  //this is the utility function for the message time
  //this is the utility function for the message time
  //this is the utility function for the message time
  //this is the utility function for the message time
  //this is the utility function for the message time
  //bascically it will convert the date and we can use it
  
